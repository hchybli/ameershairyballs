# @backstop/api — deprecated (WS-05)

Express API replaced by Supabase Edge Functions in `supabase/functions/`.

Operator and owner apps call Edge Functions via `@backstop/api-client` and read models via `@backstop/handlers`.
