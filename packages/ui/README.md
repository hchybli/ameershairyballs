# @backstop/ui

Shared shadcn/ui components and Tailwind preset for `operator` and `owner` apps.

## Planned components

- Button, Card, Badge (shadcn)
- `FlagCard` — port from legacy `claim-flags.tsx`
- `KpiTile` — owner dashboard

## Workstream

Parallel with WS-06 / WS-08
